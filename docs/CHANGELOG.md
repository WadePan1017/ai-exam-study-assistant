# 变更记录

## 2026-07-18

- 初始化 Next.js、TypeScript、Tailwind、Vitest 和 Playwright。
- 新增个人访问口令保护和30天服务端签名Cookie。
- 新增移动端底部导航、桌面侧栏和Phase 1真实空状态页面。
- 新增 Supabase 服务端连接入口及 Phase 1 数据库迁移。
- 记录个人所有者模式、考试配置、每日任务、掌握度和AI预算决策。
- 访问会话加入签发时间和服务端30天到期校验。
- 访问口令加入哈希指纹、5次失败限流和最高1小时指数退避；
  正式环境使用 Supabase 持久化，本地使用进程内回退。
- 首页日期改为按请求和上海时区计算，学习统计改为真实空数据状态。
- 初始化种子改为只插入、不覆盖已经编辑的所有者资料和考试配置。
- E2E 增加错误口令、刷新保持、退出保护和桌面侧栏覆盖。
- 将受影响的 PostCSS 依赖固定到 8.5.10，生产依赖审计无已知漏洞。
